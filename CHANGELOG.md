# 변경 이력 (CHANGELOG)

뉴월드 EDMS · `captin715/newworld-edms` · https://edms.newworldkr.com

**규칙 (C-43)**

- 최신 항목이 맨 위입니다.
- 배포 청구 시 EDMS_제작창이 이 파일의 갱신본을 함께 냅니다.
- 항목 형식: **날짜 · 커밋 해시 · 결함번호/확정사항 · 무엇을 왜 · 검증 · 영향**
- 웹 업로드로 올리면 커밋 메시지가 `Add files via upload` 로 남습니다. **커밋 메시지는 나중에 고치지 않습니다(이력 조작).** 대신 이 파일에 설명을 붙입니다.

---

## 2026-08-04 · `753ace5` · DEF-39 / C-41

### 결재선 기본값 고정 + 작성자=검토자 상신 차단 (`establish.html`)

**문제** — 검토자 기본값이 `품질팀장`으로 떠 작성자와 같아지는 상태였습니다. `pickReviewer` 가 계열이 Q·D·DS 가 아니면 검토자를 `reviewer`(품질팀장)로 골랐는데, 작성 권한자도 품질팀장이라 대부분의 계열에서 기본값이 「작성자 = 검토자」가 됐습니다. 그대로 상신하면 문서관리절차서 §5.2(작성·검토 분리 SHALL)를 위반한 문서가 결재선을 타고, 매 문서마다 사람이 수동으로 고쳐야 했습니다.

**수정**

- `pickReviewer` 가 본인이 아닌 관리자를 우선 선택 (C-41: 작성 품질팀장 → 검토 부대표 → 승인 대표이사). 사용자 변경은 여전히 가능.
- 개별 상신과 CFSPS 일괄 상신 **양 경로**에 SoD 차단 추가. 조건: 작성자=검토자 / 작성자=승인자 / 검토자=승인자. **경고가 아니라 차단.**
- 안내 문구를 C-41 표기 + §5.2 경고로 교체.

**검증** — 품질팀장 계정으로 실제 렌더해 `#fReviewer` 선택값을 판독.

```
기준본  작성 품질팀장 → 검토 품질팀장 → 승인 대표이사   FAIL
수정본  작성 품질팀장 → 검토 부대표   → 승인 대표이사   PASS
```

회귀 대조 결과 기존 동작 변동 없음. 런타임 오류 양쪽 0건.
**미입증** — SoD 차단 3조건이 실제 상신을 막는지는 시뮬로 태우지 못했습니다(상신은 RPC 호출·파일 업로드를 동반). 분기 삽입까지가 입증 범위입니다.

**영향** — 문서 제·개정 착수 화면만. 기존 결재 108건 무영향.
서버(`edms_apv_create`)에는 아직 SoD 검사가 없어 RPC 직접 호출은 우회 가능 — 별건으로 초안 제출.

---

## 2026-08-04 · `005daa1` · DEF-30 / DEF-37(화면) / DEF-38 / P7

### 결재 버전 열 · 낱장 상한 안내 · 마감 문서철 제외 · 양식대장 정렬 (`records.html`, `establish.html`)

**DEF-30** — 결재 이력 화면이 존재하지 않는 컬럼 `a.version` 을 읽어 **108건 전부 공란**이었습니다. `a.target_version` 으로 정정. 데이터는 108/108 채워져 있었습니다. 결재 대상 버전은 ISO 필수 기록입니다.

**DEF-37(화면)** — 낱장 채번 충돌 안내가 「잠시 후 다시 시도해 주세요」였습니다. `record_no` 는 낱장 3자리(`lpad(seq,3,'0')`)이고 `lpad('1000',3,'0')='100'` 으로 **잘립니다.** 1000번째 낱장은 100번째와 번호가 같아져 `UNIQUE` 충돌이 나므로 **재시도해도 영구 실패**입니다. 상한 도달과 동시 등재 충돌을 문구로 분리했습니다.

**DEF-38** — 문서철 선택 목록이 `status` 를 거르지 않아 **마감(`closed`) 문서철을 고를 수 있었고**, 고르면 서버가 「유효한 문서철이 아닙니다」로 거절했습니다. 목록에서 제외.

**P7** — 양식 대장 기본 정렬(`dept_code` → `form_seq`) 추가. 종전에는 DB 반환 순서라 새로고침마다 달라질 수 있었습니다. *(행수 표시는 이미 구현되어 있었습니다 — 제작창의 종전 제안이 부정확했습니다.)*

**검증** — `records.html` 시뮬 PASS 18 / FAIL 0 / 런타임 오류 3계정 0건. `establish.html` 은 기준본과 완전 동일(회귀 없음)까지 확인.

**영향** — 결재함·기록 등재·양식 대장 화면.

---

## 2026-08-04 · `9ae0321` · DEF-35

### 기록 등재 접근구분 값 정정 + `read_scope` 축 분리 (`records.html`)

**문제** — 화면은 `all` / `need` 를 보내는데 DB CHECK 허용값은 `dept` / `need_to_know` 뿐이었습니다. 게다가 같은 값 하나를 `p_read_scope`(P4 구획 축)와 `p_access`(공개 범위 축) **두 축에 동시에** 넣고 있었습니다.

**→ 「전사」를 골라도, 「need-to-know」를 골라도 CHECK 위반으로 등재가 실패했습니다.** 차단이 안 되는 게 아니라 **등재가 아예 안 되는** 상태였습니다. 기록이 2건뿐이던 원인입니다.

**수정**

- `rrAccess` 값 → `dept` / `need_to_know` (CHECK 허용값)
- `p_read_scope` → 선택한 문서철에서 **상속** (P4 구획은 문서철 단위 정책, 판정 20260803_10)
- 상세 표시에서 `read_scope` 폴백 제거(다른 축), 폼 초기화·안내문 토글 값 정정

**검증** — 시뮬 PASS 18 / FAIL 0. 배포 후 실제 RPC 를 트랜잭션 안에서 호출해 채번·INSERT·감사기록 전 구간 통과 확인(커밋하지 않고 롤백).
**미입증** — 브라우저→RPC 구간(파일 업로드 포함)은 태우지 못했습니다.

**영향** — 기록 등재 화면. 기존 기록 2건 무변동.

---

## 2026-08-03 · `f17e8de` · 시스템 개칭

### 문서양식 제정시스템 → 문서 제·개정 시스템

`common.js` 의 `SYS.establish.name` 1줄 변경과 각 화면의 표기 반영. 대문 2단 개편(서브 대문 2본 신설) 포함.
